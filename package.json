{
  "name": "fundraiser-app",
  "version": "1.0.0",
  "description": "Fundraiser React Application with Backend",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../client && npm install",
    "start": "cd backend && node server.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd client && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
